<!-- mneme-start v1.0 -->
# Mneme (Qoder)

Qoder reads `QODER.md` for project context and supports MCP servers via
`.qoder/mcp.json`. Mneme is exposed there as the `mneme` server,
backed by the per-user `mneme-supervisor` daemon.

## Tool usage rules (MANDATORY)

**Call mneme MCP tools BEFORE Qoder's built-in code search, file
read, or workspace tools** for structural or semantic questions.

On every task:

1. `mneme.minimal_context(task="<description>")` — ~100 tokens.
2. Iterate with `mneme.query`, `mneme.impact`, `mneme.search`
   at `detail_level="minimal"`.
3. Open files only as directed. Budget: <= 5 calls + <= 800 tokens.
4. Edits update the graph incrementally via the supervisor's watcher.

## Step ledger / drift / compaction

- Ledger: `~/.mneme/projects/<id>/steps.jsonl`. Recover with
  `mneme.last_steps(n=10)`.
- Address symbol/doc/test drift before declaring done.
- After any reset, re-prime with `mneme.minimal_context(...)`.
<!-- mneme-end -->
